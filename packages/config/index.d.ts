// Placeholder so `tsc -p tsconfig.base.json` has at least one matched input
// file when run standalone. This package exports config files, not runtime
// code; consuming packages provide their own sources.
export {};
